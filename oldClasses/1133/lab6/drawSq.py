#import turtle
def drawSquare(myTurtle,length,angle):
    myTurtle.left(angle)
    myTurtle.forward(length)
    myTurtle.left(90)
    myTurtle.forward(length)
    myTurtle.left(90)
    myTurtle.forward(length)
    myTurtle.left(90)
    myTurtle.forward(length)
#drawSquare(turtle.Turtle(),sideLength,90)
