/**
 * Created by schra447 on 5/3/16.
 */
/*
    O(n) since the array will have to be fully searched  through to find the smallest integer
    O(log n) since you will only need to find the bottom of the tree and then multiply based
    which row you stop in.
    O(1) since the operation of length() can be used which accesses the defined length the array was set to.
    A linked list would be a more efficient way of storing the data then a hash table since
    the table will need to be resized after a certain length, but the downside is that you lose

 */
public class FinalQuestions {
    public class test(Object 1,int n){

    }
}
